import { Text } from 'native-base';
import React from 'react';
import { SafeAreaView } from 'react-native';

const Home = () => {
	return (
		<SafeAreaView>
			<Text fontSize="lg">Hi! from Home!</Text>
		</SafeAreaView>
	);
};

export default Home;
