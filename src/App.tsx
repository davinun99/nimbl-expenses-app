import { Text } from 'native-base';
import React from 'react';
import {SafeAreaView} from 'react-native';

const App = () => {
	return(
		<SafeAreaView>
			<Text>Hi!</Text>
		</SafeAreaView>
	);
};
export default App;